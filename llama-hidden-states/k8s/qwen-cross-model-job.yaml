apiVersion: batch/v1
kind: Job
metadata:
  name: qwen-cross-model
  namespace: mltraining-dev
  labels:
    app: qwen-cross-model
    owner: amehta
    project: research
    kueue.x-k8s.io/queue-name: research-systems-h200
    kueue.x-k8s.io/priority-class: medium
  annotations:
    owner: amehta@snowflake.com
    project: research-systems-h200
spec:
  backoffLimit: 3
  completions: 1
  parallelism: 1
  template:
    metadata:
      labels:
        app: qwen-cross-model
        gpu: "true"
      annotations:
        sidecar.istio.io/inject: "false"
    spec:
      restartPolicy: OnFailure
      serviceAccountName: mltraining-dev-sa
      initContainers:
        - name: init-results-dir
          image: 937559181915.dkr.ecr.us-west-2.amazonaws.com/du/playground-generic:1.7.1
          command: ["/bin/bash", "-c"]
          args:
            - |
              mkdir -p /results/qwen_cross_model
              echo "Results directory initialized at $(date)" > /results/qwen_cross_model/init.log
          volumeMounts:
            - name: results
              mountPath: /results
      containers:
        - name: qwen-server
          image: 937559181915.dkr.ecr.us-west-2.amazonaws.com/du/playground-generic:1.7.1
          imagePullPolicy: IfNotPresent
          command: ["/bin/bash", "-c"]
          args:
            - |
              set -ex
              echo "Installing dependencies..."
              pip install -q fastapi==0.109.0 uvicorn[standard]==0.27.0 transformers==4.46.0 accelerate==0.34.2 huggingface-hub==0.26.0 pydantic==2.6.0 sentencepiece==0.2.0 protobuf==4.25.2 autoawq==0.2.7
              echo "Starting Qwen 2.5 72B-AWQ server..."
              cd /app && python -u server.py
          env:
            - name: MODEL_NAME
              value: "Qwen/Qwen2.5-72B-Instruct-AWQ"
            - name: USE_NNSIGHT
              value: "false"
            - name: USE_PIPELINE_PARALLEL
              value: "true"
            - name: HF_TOKEN
              valueFrom:
                secretKeyRef:
                  name: llama-70b-secrets
                  key: hf-token
            - name: CUDA_VISIBLE_DEVICES
              value: "0,1,2,3,4,5,6,7"
            - name: TRANSFORMERS_CACHE
              value: "/cache/huggingface"
            - name: HF_HOME
              value: "/cache/huggingface"
          startupProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 60
          ports:
            - containerPort: 8000
              name: http
          resources:
            requests:
              cpu: "32"
              memory: "400Gi"
              nvidia.com/gpu: "8"
            limits:
              cpu: "96"
              memory: "700Gi"
              nvidia.com/gpu: "8"
          volumeMounts:
            - name: cache-volume
              mountPath: /cache
            - name: dshm
              mountPath: /dev/shm
            - name: server-code
              mountPath: /app
        - name: experiment
          image: 937559181915.dkr.ecr.us-west-2.amazonaws.com/du/playground-generic:1.7.1
          imagePullPolicy: IfNotPresent
          command: ["/bin/bash", "-c"]
          args:
            - |
              set -ex
              echo "Installing experiment dependencies..."
              pip install -q requests numpy openai together anthropic httpx
              echo "Waiting for Qwen server to start..."
              cd /experiment
              python -u run_cross_model_experiment.py \
                --questions /experiment/cross_model_20q.json \
                --output /results/qwen_cross_model \
                --resume
              echo "Experiment complete!"
              echo "EXPERIMENT_DONE" > /results/qwen_cross_model/DONE
          resources:
            requests:
              cpu: "4"
              memory: "16Gi"
            limits:
              cpu: "8"
              memory: "32Gi"
          volumeMounts:
            - name: results
              mountPath: /results
            - name: experiment-code
              mountPath: /experiment
      volumes:
        - name: cache-volume
          persistentVolumeClaim:
            claimName: qwen-hf-cache
        - name: dshm
          emptyDir:
            medium: Memory
            sizeLimit: 64Gi
        - name: server-code
          configMap:
            name: llama-server-code
        - name: experiment-code
          configMap:
            name: qwen-cross-model-code
        - name: results
          persistentVolumeClaim:
            claimName: qwen-cross-model-results
      tolerations:
        - key: "nvidia.com/gpu"
          operator: "Exists"
          effect: "NoSchedule"
