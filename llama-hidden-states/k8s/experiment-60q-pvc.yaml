apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: experiment-60q-results
  namespace: mltraining-dev
  labels:
    app: experiment-60q
    owner: amehta
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: gp3
  resources:
    requests:
      storage: 50Gi
